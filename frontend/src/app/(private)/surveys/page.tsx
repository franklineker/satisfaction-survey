import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Surveys",
    description: "Dashboard of the surveys.",
};

export default function Surveys() {
    return (
        <div>
            <h1>Surveys</h1>
        </div>
    );
}
