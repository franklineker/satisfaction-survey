import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "contacts",
    description: "This page display the dashboard of Contacts.",
};

export default function Contacts() {
    return (
        <div>
            <h1>Contacts</h1>
        </div>
    );
}
