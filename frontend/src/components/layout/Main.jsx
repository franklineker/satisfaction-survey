
export default function Main({ children }) {
    return (
        <main className="gap-[32px] row-start-2 p-6 items-center sm:items-start overflow-y-auto max-h-full">
            {children}
        </main>
    )
}
